# Permanent Instructions

## GitHub Repository
- The ONLY repository to ever use is **shannon-hue/supplies-savings**
- The `mydashboard` repo has been deleted — ignore it entirely
- Never push to any other repository without explicit permission

## Project
- This is the Moxie Supplies Savings dashboard
- Local repo path: `/home/user/mydashboard-repo`

## Data Verification Standard
The user is a high-level executive. All data lookups must be verified before presenting results:
- **Never state a data finding as fact after a single query.** Always cross-check using multiple approaches (e.g. search by moxie_id AND by name, check all relevant fields, verify date parsing is correct for mixed formats).
- **Never say "no data" or "zero rows" without explicitly confirming** the search covered all possible name variants, ID matches, and date formats used in that vendor's file.
- If a number seems off or contradicts what the user sees, investigate fully before responding — do not guess or speculate.
- When presenting spend or revenue figures, state exactly how the number was calculated (which fields, which pricing formula, which rows) so it can be independently verified.
- It is far better to take an extra moment to verify than to give incorrect information that has to be corrected.
